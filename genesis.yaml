# genesis.yaml - Cigkoftecibey Webapp Configuration
# Auto-generated by ProjectAnalysisAgent (P0.63)
# Port adjusted to 5176 to avoid conflict with genesis-lab-dev (5174)

# Project: cigkoftecibey-webapp
# Framework: vite-fastapi
# Type: fullstack (monorepo)

services:
  frontend:
    # Detected: vite
    # Port: 5176 (adjusted from 5173 to avoid conflicts)
    port: 5176
    start: "cd frontend && npm run dev -- --port 5176"
    stop: "pkill -f 'vite.*cigkoftecibey'"
    health_check:
      url: "http://localhost:5176/"
      method: GET

  backend:
    # Detected: fastapi
    port: 8000
    start: "cd backend && ./venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
    stop: "pkill -f 'uvicorn.*app.main:app'"
    health_check:
      url: "http://localhost:8000/api/health"
      method: GET

# Note: Backend requires project-specific venv
# To restart backend manually:
#   cd projects/cigkoftecibey-webapp/backend
#   source venv/bin/activate
#   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Metadata
generated_by: "ProjectAnalysisAgent (Haiku 4.5) + Manual Port Adjustment"
project_type: "vite-fastapi"
confidence: 1.0
