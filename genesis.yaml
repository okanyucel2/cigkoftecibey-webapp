# genesis.yaml - Cigkoftecibey Webapp Configuration
# GENESIS Process Manager v2.0 (Smart Port Allocation)
#
# Project: cigkoftecibey-webapp
# Framework: vite-fastapi
# Type: fullstack (monorepo)
#
# Port Allocation:
#   - Backend: Auto-allocated (hash-based deterministic)
#   - Frontend: Auto-allocated (hash-based deterministic)

services:
  frontend:
    # Detected: vite
    start: "cd frontend && npm run dev"
    stop: "pkill -f 'vite.*cigkoftecibey'"
    # health_check url will be auto-generated

  backend:
    # Detected: fastapi
    start: "cd backend && ./venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0"
    stop: "pkill -f 'uvicorn.*app.main:app'"
    # health_check url will be auto-generated (expects /api/health)

# Note: Backend requires project-specific venv
# To restart backend manually:
#   cd projects/cigkoftecibey-webapp/backend
#   source venv/bin/activate
#   uvicorn app.main:app --reload --host 0.0.0.0

# Metadata
generated_by: "GENESIS Process Manager v2.0 (Smart Port Allocation)"
project_type: "vite-fastapi"
confidence: 1.0
