# Railway Multi-Service Configuration
# Docs: https://docs.railway.app/reference/config-as-code

[build]
# Root level - no build needed here
builder = "nixpacks"

[deploy]
# Default settings
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
